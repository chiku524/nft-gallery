# Strangers OpenSea pack

24 unique looping paintings at 512×512, 12 frames, 90ms. Each artwork is an open edition.

Create an OpenSea Open Edition collection on Base. Upload every file in `gifs/` plus `opensea-metadata.csv`. OpenSea plays GIF, not APNG. The site keeps the APNGs in `public/strangers/`.
