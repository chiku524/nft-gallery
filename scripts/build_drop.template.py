"""Blank painter for the next collection.

Copy this file to `scripts/build_<slug>.py` only after the drop is named.
Do not import `build_scribblins`, `build_foxins`, or any other living painter
for art, palette, skeleton, or layer names. Import `paint_kit` for I/O only.

Invent the look from scratch: subject, silhouette, materials, palette,
layer language, and motion.
"""

from __future__ import annotations

# from paint_kit import DURATION_MS, FRAMES, SIZE, place_portrait, save_apng, save_image

# TODO: lock name, ticker, subject, chain, supply, mint price before painting.
# TODO: invent a new drawing language. Do not reuse doodle ink, sticker cutouts,
#       oval-egg bodies, or Field/Body/Mug/Hat/Wrap/Charm.


def main() -> None:
    raise SystemExit("This is a template. Copy it to scripts/build_<slug>.py and invent a new look.")


if __name__ == "__main__":
    main()
